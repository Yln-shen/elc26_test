# 项目日志

---

## 2026-07-29 — 第一次对话

### 当前目标

用 YOLO 识别在半截面水管上滚动的小钢珠（直径 1cm，银色金属），从多
个检测结果中筛选出唯一真实目标，用于后续电机 / 激光控制。

### 已完成的改动

- **`src/vision/detector.py`** — 完全重写
  - 删除了传统矩形检测全部代码：`Board` 类、`angle_between_lines()`、
    `process()`（OTSU 二值化）、`find_boards()`（轮廓查找）、
    `select_board()`（平行度评分选板）、`order_points()`、
    `compute_parallel_score()`、`draw_boards()`、PNP 位姿估计
  - 新增 YOLO（ultralytics）推理，加载 RKNN 模型
  - 新增筛选逻辑：
    - 主键：钢珠中心 y 坐标与图像中心 y 差值最小
    - 次键（平局）：钢珠中心 x 坐标与图像中心 x 差值最小
  - 新增 `draw()` 方法：
    - 选中的钢珠：绿色粗框 + 实心圆心 + 坐标标注 + 画面中心连线
    - 其他检测（误识别）：红色细框

### 待实现 / 待确认

- 在真实设备（Radxa）上跑通 `detector.py` 的 YOLO 推理
- YOLO 模型路径确认：开发机 `/home/yln/elc26_test/...` vs 设备
  `/home/radxa/elc26_test/...`
- 筛选逻辑在实际误识别场景下是否足够鲁棒
- 钢珠中心坐标如何传入电机 / 激光控制模块
- `main.py`（根目录下）目前是占位文件，需要对接 `Detector`

