# 项目日志

---

## 2026-07-29 — 第一次对话

### 当前目标

用 YOLO 识别在半截面水管上滚动的小钢珠（直径 1cm，银色金属），从多
个检测结果中筛选出唯一真实目标，用于后续电机 / 激光控制。

### 已完成的改动

- **`src/vision/detector.py`** — 完全重写
  - 删除了传统矩形检测全部代码：`Board` 类、`angle_between_lines()`、
    `process()`（OTSU 二值化）、`find_boards()`（轮廓查找）、
    `select_board()`（平行度评分选板）、`order_points()`、
    `compute_parallel_score()`、`draw_boards()`、PNP 位姿估计
  - 新增 YOLO（ultralytics）推理，加载 RKNN 模型
  - 新增筛选逻辑：
    - 主键：钢珠中心 y 坐标与图像中心 y 差值最小
    - 次键（平局）：钢珠中心 x 坐标与图像中心 x 差值最小
  - 新增 `draw()` 方法：
    - 选中的钢珠：绿色粗框 + 实心圆心 + 坐标标注 + 画面中心连线
    - 其他检测（误识别）：红色细框

### 待实现 / 待确认

- 在真实设备（Radxa）上跑通 `detector.py` 的 YOLO 推理
- YOLO 模型路径确认：开发机 `/home/yln/elc26_test/...` vs 设备
  `/home/radxa/elc26_test/...`
- 筛选逻辑在实际误识别场景下是否足够鲁棒
- 钢珠中心坐标如何传入电机 / 激光控制模块
- `main.py`（根目录下）目前是占位文件，需要对接 `Detector`

---

## 2026-07-29 — 第二次对话：低延迟 + 只画选中球

### 改动

- **`src/vision/detector.py`** — 第二轮优化
  - `detect()`：去掉 `all_detections` 列表，inline 直接追踪 best ball，
    不再构建中间 detection 列表
  - `draw()`：
    - 不再 `frame.copy()`，直接画在原帧上
    - 不再遍历所有检测框 — 只画 `_box`（选中的那一颗）
    - 去掉 `Objects: N` / `Selected:` 文字，只保留坐标标注
    - 缩小圆心点尺寸（8→6 filled, 10→8 ring）

### 延迟优化要点

| 优化项 | 效果 |
|--------|------|
| 取消中间 detection 列表 | 省去 list append + 内存分配 |
| inline best-ball 搜索 | 单次遍历，不构建数据结构 |
| `draw()` 不 copy 帧 | 省去 640×480×3 的 memcpy |
| `draw()` 不遍历 | O(1) 绘制，与检测数无关 |

---

## 2026-07-29 — camera.py 低延迟优化

### 改动

- **`src/vision/camera.py`**
  - **`cv2.CAP_PROP_BUFFERSIZE = 1`** — 核心优化。V4L2 驱动默认缓冲 4-6 帧，
    摄像头 120fps（~8ms/帧）持续写入，YOLO 推理 ~20ms，消费端每次 `grab()`
    拿到的是队列里积压的旧帧（滞后可达 30ms+）。设为 1 后每次 `grab()` 返回
    传感器最新帧，端到端延迟降一个数量级
  - 启动时 `grab()` 5 次冲刷初始缓冲区（刚打开的帧往往偏暗/过曝）
  - `_grab` / `_retrieve` 方法引用缓存，省去属性查找
  - `_find_cam` 改为 `@staticmethod`

### 延迟构成（更新估计）

| 环节 | 优化前 | 优化后 |
|------|--------|--------|
| 驱动缓冲滞后 | 0-30ms（不可控） | 0-8ms（最多 1 帧） |
| V4L2 grab | ~0.2ms | ~0.2ms |
| MJPG decode | ~1-2ms | ~1-2ms |
| YOLO NPU | ~10-30ms | ~10-30ms |

---

## 2026-07-29 — detector.py 第三轮微优化

### 改动

- **`src/vision/detector.py`**
  - 手动标量比较替代元组构造（每轮循环省 `(abs(dy), abs(dx))` tuple 分配）
  - `dy < best_dy` 提前胜出时跳过 x 计算（大多数迭代省 1 次 abs+除法）
  - `__init__` 中缓存 5 个 cv2 函数引用（`_rect`, `_circle`, `_line`, `_putText`, `_font`）
  - `cv2.drawMarker` → 2× `cv2.line`（省去 drawMarker 内部 switch 分发）
  - 去掉置信度文字渲染（putText 是最贵的 OpenCV 绘制调用之一）

---

## 2026-07-29 — detector.py 第四轮：ROI 裁剪

### 动机

用户反馈误识别主要来自水管以外区域（桌面、反光等）。相机和水管位置固定，
天然适合 ROI 裁剪。

### 改动

- **`src/vision/detector.py`**
  - `__init__` 新增 `roi` 参数：`(x, y, w, h)` 或 `None`（不裁剪）
  - `detect()`：
    - 有 ROI 时裁出 `frame[ry:ry+rh, rx:rx+rw]` 送入 YOLO
    - YOLO 返回的 ROI 坐标通过 `+rx, +ry` 映射回全帧坐标
    - 筛选用的 `frame_center` 始终是全帧中心（不随 ROI 改变）
    - **NPU 计算量不变**（ROI 被 ultralytics resize 到 640×640），但背景噪声
      大幅减少 → 误识别从源头消除
  - `draw()`：新增灰色虚线框显示 ROI 区域，便于调试

### ROI 效果

| 维度 | 无 ROI | 有 ROI |
|------|--------|--------|
| YOLO 视野 | 整帧 640×480（含桌面、反光） | 只含水管道区域 |
| 误识别 | 来自背景 | 从源头消除 |
| NPU 计算 | 640×640 | 640×640（不变） |
| 钢珠有效分辨率 | 原始 | ROI 放大填充 → 钢珠更清晰 |

### 如何调 ROI

`detector.py` 底部 `PIPE_ROI = (x, y, w, h)` 四个值需根据实际画面调节。
原则：覆盖整个水管长度 + 钢珠上下滚动的范围，留少量 margin。

---

## 2026-07-29 — 诊断：检测不稳定 & 阈值修复

### 问题

小钢珠一会识别得到一会不行。

### 诊断过程

- `detector.py` 新增 `debug=True` 模式：
  - YOLO 内部阈值降到 0.05，显示所有 raw 检测框
  - 红框 = 低于阈值被丢弃；黄框 = 高于阈值未选中；绿框 = 选中
  - 显示当前帧最高置信度 `max conf`
- 运行观察：**红框稳定框住钢珠，conf 在 0.05-0.19 波动**

### 根因

模型本身没问题（每帧都能检测到钢珠），是 `conf_threshold=0.20` 刚好卡在
钢珠置信度波动区间的上沿 → 置信度低于 0.20 的帧被当作"未检测"。

### 修复

- `conf_threshold` 从 0.20 降到 **0.10**
- 配合 ROI 裁剪（已排除桌面反光等误识别来源），低阈值不会引入新的误识别
- `debug` 默认关闭，需要时手动开启

